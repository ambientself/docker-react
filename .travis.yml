language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ambientself/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ambientself/docker-react npm run test
